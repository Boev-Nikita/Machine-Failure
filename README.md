# Предсказание поломки оборудования

**Цель проекта:** Нужно предсказать, произойдёт ли отказ оборудования на основе его технических характеристик, условий эксплуатации и истории работы.

---

## 📊 Расшифровка столбцов датасета

### Идентификаторы
* **id** — Уникальный числовой идентификатор записи.
* **Product ID** — Идентификатор продукта/оборудования (формат: буква + цифры).
* **Type** — Тип оборудования (категория: `L`, `M`, `H` — *Low/Medium/High*).

### Технологические параметры
* **Air temperature [K]** — Температура окружающего воздуха в Кельвинах.
* **Process temperature [K]** — Технологическая температура процесса в Кельвинах.
* **Rotational speed [rpm]** — Скорость вращения в оборотах в минуту.
* **Torque [Nm]** — Крутящий момент в Ньютон-метрах.
* **Tool wear [min]** — Износ инструмента в минутах работы.

### Признаки отказов
* **Machine failure** — Общий индикатор отказа оборудования (`1` — отказ, `0` — норма).
* **TWF** (*Tool Wear Failure*) — Отказ из-за износа инструмента.
* **HDF** (*Heat Dissipation Failure*) — Отказ теплоотвода.
* **PWF** (*Power Failure*) — Отказ питания.
* **OSF** (*Overstrain Failure*) — Отказ из-за перегрузки.
* **RNF** (*Random Failures*) — Случайные отказы.

---

## 📈 Выводы

### 1. Исследовательский анализ данных
Проведен первичный анализ данных.

### 2. Генерация базовых признаков
Созданы новые аналитические признаки:
* `HDF_temp` — разница признаков `Process temperature [K]` и `Air temperature [K]`.
* `PWF_n` — произведение признаков `Torque [Nm]` и `Rotational speed [rpm]`.
* `OSF_n` — произведение признаков `Torque [Nm]` и `Tool wear [min]`.

### 3. Выявленные зависимости
Обнаружены следующие ключевые различия в распределении признаков между нормальным состоянием (`0`) и отказом (`1`) по интервалам Q25-Q75:

* **3.1 TWF — Отказ из-за износа инструмента:**
  * `Tool wear [min]` → **0:** `53-162` | **1:** `198-262`
* **3.2 HDF — Отказ теплоотвода:**
  * `Air temperature [K]` → **0:** `298.3-301.5` | **1:** `302.1-303.075`
  * `Rotational speed [rpm]` → **0:** `1426-1614` | **1:** `1259-1379`
  * `Torque [Nm]` → **0:** `33.1-46.6` | **1:** `48.2-57.1`
  * `HDF_temp` → **0:** `9.3-11.0` | **1:** `7.6-8.6`
* **3.4 PWF — Отказ питания:**
  * `Rotational speed [rpm]` → **0:** `1424-1611` | **1:** `1312-2563.75`
  * `Tool wear [min]` → **0:** `52-161` | **1:** `172-238`
  * `PWF_n` → **0:** `885.6-1112.2` | **1:** `546.8-1494.0`
* **3.5 OSF — Отказ из-за перегрузки:**
  * `Tool wear [min]` → **0:** `52-161` | **1:** `172-238`
  * `Torque [Nm]` → **0:** `33.1-46.6` | **1:** `46.3-70.5`
  * `OSF_n` → **0:** `1846.87-6232.8` | **1:** `110032.8-164790.0`

### 4. Создание категориальных признаков
Для улучшения работы моделей сгенерированы следующие флаги:
* `TW_cat` — износ `Tool wear [min]` больше или меньше 198.
* `AT_cat` — температура `Air temperature [K]` больше или меньше 301.6.
* `HDF_cat` — комбинация условий: `HDF_temp` (больше/меньше 8.7) и `Rotational speed [rpm]` (больше/меньше 1380).
* `PWF_cat` — находится ли `PWF_n` в интервале от 885.5 до 1112.3.

### 5. Отбор признаков
Проведен корреляционный анализ данных. Все избыточные признаки с высокой мультиколлинеарностью были удалены из финального датасета.

---

## 🤖 Моделирование

Обучено несколько моделей с перебором гиперпараметров. 
* **Целевой признак:** `Machine failure`
* **Целевая метрика:** `F1-score` (для оптимального баланса между Precision и Recall).

Лучшие результаты показала модель **LGBMClassifier**.

<details>
<summary>⚙️ Кликните, чтобы посмотреть гиперпараметры модели</summary>

```python
LGBMClassifier(
    boosting_type='gbdt', num_leaves=118, max_depth=2, 
    learning_rate=0.2, n_estimators=125, random_state=42,
    subsample_for_bin=200000, min_child_samples=1, verbose=-1
)
```
</details>

### 🎯 Метрики на тестовой выборке

| Метрика | Значение |
| :--- | :--- |
| **F1-score** | **0.7974** |
| **F2-score** | 0.7475 |
| **Accuracy** | 0.9876 |
| **Precision** | 0.8971 |
| **Recall** | 0.7176 |
| **ROC AUC** | 0.8574 |
| **MCC** | 0.7960 |

---

## 🧮 Важность признаков (Feature Importance)

1. **Rotation speed (0.91)** — Ключевой фактор. Чем меньше скорость вращения, тем выше риск поломки.
2. **PWF_cat (0.72)** — Если флаг равен `1`, вероятность отказа значительно возрастает.
3. **Tool Wear (0.52)** — Прямая зависимость: чем выше износ инструмента, тем выше риск поломки.
4. **HDF_temp (0.31)** — Чем ниже разница температур, тем выше риск поломки.
5. **Process Temperature (0.21)** — Чем ниже технологическая температура процесса, тем выше риск поломки.
